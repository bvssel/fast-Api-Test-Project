version: "3.7"

services:
  app:
    build: .
    container_name: app
    volumes:
      - ./:/src/
    ports:
      - 8082:8000
    # environment:
    #   - DATABASE_URL=postgresql://fastapi:fastapi@db/fastapi